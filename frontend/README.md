# Word to PDF frontend

## Development

- `npm ci`
- `npm start:dev`
- Drag and drop the `dist` folder to Chrome extensions in the dev mode
- Reload the extension in the Chrome extensions settings when the `dist` folder is updated
